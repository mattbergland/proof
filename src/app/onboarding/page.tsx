"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { Button, Field } from "@/components/ui";
export default function Onboarding(){const router=useRouter();const[name,setName]=useState("");const[website,setWebsite]=useState("");const[busy,setBusy]=useState(false);return <main className="mx-auto max-w-xl px-6 py-16"><Link href="/" className="text-xl font-semibold tracking-[-.04em]">proove.now</Link><div className="mt-20"><p className="text-sm uppercase tracking-[.16em] text-[#b4540a]">Set up your workspace</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.06em]">Make proof part of the workflow.</h1><p className="mt-5 text-lg leading-8 text-[#726d63]">A couple details, then you can send your first customer capture.</p><form className="mt-10 grid gap-5" onSubmit={async e=>{e.preventDefault();setBusy(true);await fetch("/api/onboarding",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,website})});router.push("/dashboard")}}><Field label="Company name" value={name} onChange={e=>setName(e.target.value)} required placeholder="Fieldline"/><Field label="Company website" value={website} onChange={e=>setWebsite(e.target.value)} placeholder="https://fieldline.dev"/><Button disabled={busy}>{busy?"Setting up…":"Continue"}</Button></form></div></main>}
