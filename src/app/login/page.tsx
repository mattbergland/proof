"use client";
import { useState } from "react"; import Link from "next/link"; import { Button, Field } from "@/components/ui";
export default function Login(){const[email,setEmail]=useState("demo@fieldline.dev");const[link,setLink]=useState("");const[loading,setLoading]=useState(false);return <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center px-6"><Link href="/" className="mb-14 text-xl font-semibold tracking-[-.04em]">proove.now</Link><h1 className="text-4xl font-semibold tracking-[-.05em]">Welcome back.</h1><p className="mt-3 text-[#726d63]">Sign in with a magic link. No password to remember.</p><form className="mt-8 grid gap-5" onSubmit={async e=>{e.preventDefault();setLoading(true);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const d=await r.json();setLink(d.link||"");setLoading(false)}}><Field label="Work email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><Button disabled={loading}>{loading?"Sending…":"Send magic link"}</Button></form>{link&&<div className="mt-6 border border-[#d9c6a8] bg-[#fff8ea] p-4 text-sm"><strong>Dev mode: click to sign in</strong><br/><a className="mt-2 inline-block underline" href={link}>{link}</a></div>}<p className="mt-10 text-sm text-[#726d63]">New to proove.now? <Link href="/onboarding" className="text-[#191815] underline">Create a workspace</Link></p></main>}
