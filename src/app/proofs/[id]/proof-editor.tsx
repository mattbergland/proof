"use client";
import { useState } from "react"; import { Button, Field } from "@/components/ui"; import { permissionOptions } from "@/lib/templates";
export default function ProofEditor({proof}:{proof:{id:string;quote:string;permission:string;impactMetric:string|null;product:string|null;useCase:string|null;archived:boolean}}){const[quote,setQuote]=useState(proof.quote);const[permission,setPermission]=useState(proof.permission);const[metric,setMetric]=useState(proof.impactMetric||"");const[product,setProduct]=useState(proof.product||"");const[useCase,setUseCase]=useState(proof.useCase||"");const[saved,setSaved]=useState(false);async function save(archive=false){await fetch(`/api/proofs/${proof.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({quote,permission,impactMetric:metric,product,useCase,archived:archive})});setSaved(true);setTimeout(()=>setSaved(false),1200)}return <section className="mt-10 border-t border-[#dfdbd2] pt-8"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">Proof Card fields</h2>{saved&&<span className="text-sm text-[#b4540a]">Saved</span>}</div><div className="mt-5 grid gap-5 md:grid-cols-2"><label className="grid gap-2 text-sm font-medium md:col-span-2">Quote<textarea value={quote} onChange={e=>setQuote(e.target.value)} className="min-h-24 border border-[#cfc9be] bg-white px-3 py-2.5 font-normal"/></label><Field label="Impact metric" value={metric} onChange={e=>setMetric(e.target.value)} placeholder="~40% faster migrations"/><Field label="Product" value={product} onChange={e=>setProduct(e.target.value)} placeholder="Developer Platform"/><Field label="Use case" value={useCase} onChange={e=>setUseCase(e.target.value)} placeholder="Migration"/><label className="grid gap-2 text-sm font-medium">Permission<select value={permission} onChange={e=>setPermission(e.target.value)} className="border border-[#cfc9be] bg-white px-3 py-2.5 font-normal">{permissionOptions.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label></div><div className="mt-6 flex flex-wrap gap-4"><Button onClick={()=>save()}>Save changes</Button><button onClick={()=>save(true)} className="text-sm underline">{proof.archived?"Unarchive":"Archive"}</button></div></section>}
